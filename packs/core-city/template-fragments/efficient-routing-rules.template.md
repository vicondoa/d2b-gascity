{{ define "efficient-routing-rules" -}}
## Routing rules

Use the official Gas City roles and formulas. Create or inspect the governing
bead, then dispatch the formula or named role that owns the work.

Model tiers select providers for configured roles. They are not agent names,
routing targets, or bead metadata values. Do not create replacement agents
when an imported role already owns the task.
{{- end }}
