{{ define "sdlc-mayor-coding-rules" -}}
## Implementation and publication

Do not implement source changes in the mayor session. Use official formulas
and roles so implementation runs in the rig workspace with its configured
provider and review path.

Keep externally visible publication behind the repository's human approval
and pull-request policy. Never merge or force-push.
{{- end }}
