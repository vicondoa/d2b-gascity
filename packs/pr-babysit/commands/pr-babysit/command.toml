description = """
Manage durable target-only pull-request handoffs, checkpoints, bounded repair
claims, formula attachment, validation results, and confirmation in Beads.
`dispatch-repair` fences the action claim and attaches one repair formula;
`record-repair-result` records only safe SHA, make-check, and addressed-thread
identifiers; `confirm-action` closes the action child after the pushed SHA is
verified. Repair dispatch requires operator-attested least-privilege GitHub
capability and rejects coupled Copilot credentials. `check-credentials` only
checks the operator capability and credential-isolated validator attestations
plus token separation; it never introspects fine-grained permissions.
"""
run = "run.sh"
