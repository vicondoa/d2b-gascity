# Initial agent surface

The initial city intentionally imports upstream role behavior instead of
copying agent bodies. Local agent additions belong here only after their
provider and lifecycle contracts are defined.
